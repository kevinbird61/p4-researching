# 1. Make sure your current directory is in ./scripts/
# 2. Run the Bazel installer as follows
$ chmod +x bazel-0.20.0-installer-linux-x86_64.sh
$ ./bazel-0.20.0-installer-linux-x86_64.sh --user

# 3. Set up your environment or add the following command into ~/.bashrc
$ export PATH="$PATH:$HOME/bin"

# 4. If you add the above command into ~/.bashrc, then do the following command
$ . ~/.bashrc